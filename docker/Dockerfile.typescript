FROM oven/bun:1-alpine

WORKDIR /app

COPY typescript /app/typescript
WORKDIR /app/typescript

RUN bun install && bun run build

EXPOSE 8765

ENV HOST=0.0.0.0
ENV PORT=8765

CMD ["bun", "run", "examples/mcp-tool-server.ts"]
